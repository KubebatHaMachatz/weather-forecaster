import { PlaceholderScreen } from '../components/PlaceholderScreen'

export default function TutorialScreen() {
  return (
    <PlaceholderScreen
      title="Tutorial Call"
      note="A fixed, pre-scored walkthrough Call lands in a follow-up PR."
    />
  )
}
