import { PlaceholderScreen } from '../components/PlaceholderScreen.js'

export default function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      note="Unit override (DESIGN §13.1) and notification preferences land in a follow-up PR."
    />
  )
}
