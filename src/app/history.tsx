import { PlaceholderScreen } from '../components/PlaceholderScreen.js'

export default function HistoryScreen() {
  return (
    <PlaceholderScreen
      title="History"
      note="Past Calls, skill score, streaks, and calibration curve land in a follow-up PR."
    />
  )
}
